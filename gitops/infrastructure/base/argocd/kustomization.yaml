apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  # Upstream ArgoCD install manifest — pinned to a specific version.
  # To upgrade: update the version tag and run `flux reconcile kustomization infrastructure`.
  - https://raw.githubusercontent.com/argoproj/argo-cd/v2.14.4/manifests/install.yaml
